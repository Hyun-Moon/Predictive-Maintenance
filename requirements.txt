# =============================================================================
# 설비 예지보전 프로젝트 - 기본 패키지
#
# 설치:  pip install -r requirements.txt
#
# 버전을 ">=" 로 느슨하게 잡은 이유:
#   너무 딱 고정하면(==) 나중에 다른 PC/OS 에서 설치가 깨지는 경우가 많습니다.
#   대신 "이 버전 이상이면 코드가 동작한다"는 하한선만 명시했습니다.
#   실제로 설치된 정확한 버전은 requirements.lock.txt 에 기록해 둡니다.
# =============================================================================

# --- 데이터 처리 ---------------------------------------------------------
numpy>=1.26
pandas>=2.1
scipy>=1.11
pyarrow>=14.0            # parquet 저장/읽기 (CSV보다 빠르고 용량이 작음)

# --- 시각화 --------------------------------------------------------------
matplotlib>=3.8
seaborn>=0.13

# --- 머신러닝 ------------------------------------------------------------
scikit-learn>=1.4        # 전처리, 기본 모델, 평가지표
lightgbm>=4.1            # 부스팅 트리 (표 형태 데이터에서 가장 강력한 편)

# --- 유틸 ----------------------------------------------------------------
pyyaml>=6.0              # config.yaml 읽기
tqdm>=4.66               # 진행바
joblib>=1.3              # 학습된 모델 저장/불러오기
requests>=2.31           # 데이터셋 다운로드

# --- 노트북 --------------------------------------------------------------
jupyterlab>=4.0
ipykernel>=6.29

# --- 테스트 --------------------------------------------------------------
pytest>=8.0
