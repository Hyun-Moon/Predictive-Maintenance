# =============================================================================
# 딥러닝 전용 패키지 (프로젝트 후반부 - LSTM/1D-CNN 기반 RUL 모델에서만 필요)
#
# 설치:  pip install -r requirements-deep.txt
#
# 왜 파일을 나눴나?
#   PyTorch 는 용량이 매우 큽니다(CPU 버전도 200MB+, GPU 버전은 2GB 이상).
#   EDA ~ 트리 모델 단계까지는 전혀 필요 없기 때문에,
#   "딥러닝을 실제로 쓰는 시점"에만 따로 설치하도록 분리했습니다.
#
# CPU 전용으로 가볍게 설치하려면 (GPU 없는 노트북 권장):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# =============================================================================

torch>=2.2
